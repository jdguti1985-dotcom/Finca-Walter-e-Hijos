rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Solo usuarios autenticados con Google pueden leer y escribir
    match /finca/{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
